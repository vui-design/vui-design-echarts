<template>
  <div>
    <h1>介绍</h1>
    <p><code>Vui Design ECharts</code> 是基于 <code>Vue2.x</code> 与 <code>ECharts5.x</code> 构建封装的组件库，用以解决繁杂的 <code>ECharts</code> 配置项以及数据转化带来的烦恼。<code>Vui Design ECharts</code> 生成一个 <code>ECharts</code> 图表时，用户只需关心<b>数据源</b>、<b>维度</b>、<b>指标</b>，以及一些<b>少量配置项</b>，甚至无需配置项，即可生成一个默认的图表，使用 <code>Vui Design ECharts</code> 助你快捷、高效地构建图表。</p>
    <h2 id="example-introduce-example">示例</h2>

  </div>
</template>

<script>
  import Anchors from "app/mixins/anchors";

  export default {
    mixins: [
      Anchors
    ]
  };
</script>